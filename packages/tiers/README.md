# @marker/tiers

Tier classification types for Marker agent actions. Every action an agent can take is classified as read-only, reversible, or irreversible. Irreversible actions require human approval. This package provides the type definitions and classification helpers.
