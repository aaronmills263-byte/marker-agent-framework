# @aaronmills263-byte/evals

Promptfoo harness wrapper for Marker agent evaluations. Provides a typed interface for defining eval suites and running them against agent outputs. Every agent must have evals before shipping.
