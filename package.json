{
  "name": "marker-agent-framework",
  "version": "0.1.0",
  "private": true,
  "description": "Shared agent infrastructure for the Marker family",
  "scripts": {
    "build": "pnpm -r run build",
    "kill-all": "tsx scripts/kill-all.ts",
    "drill-kill-switch": "tsx scripts/drill-kill-switch.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "pnpm": "^9.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
