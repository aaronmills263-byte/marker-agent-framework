/**
 * Emergency disable — kills all running Marker agents.
 * Stub: actual implementation in a later prompt.
 */

console.log("[kill-all] Emergency kill switch activated (stub — no-op)");
process.exit(0);
